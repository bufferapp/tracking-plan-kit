traits:
  - name: email
    description: What's the user's email?
    type: string
    allowNull: true
  - name: isPayingUser
    description: Does the user have an active paid subscription?
    type: boolean
    allowNull: true